# Scouter Environment Configuration
# Copy this file to .env and update with your values

# ===========================================
# DEVELOPMENT CONFIGURATION (MailHog)
# ===========================================
# For development, no environment variables are needed.
# Just start MailHog: mailhog &
# The system will automatically use MailHog on localhost:1025

# ===========================================
# PRODUCTION CONFIGURATION (Amazon SES)
# ===========================================
# Uncomment and set these values for production:

# Environment
# FLASK_ENV=production

# Database (PostgreSQL for production)
# DATABASE_URL=postgresql://username:password@localhost:5432/scouter

# Email (Amazon SES)
# AWS_SES_USERNAME=your_ses_smtp_username
# AWS_SES_PASSWORD=your_ses_smtp_password
# FROM_EMAIL=noreply@yourdomain.com

# Optional Email Settings (use defaults if not set)
# MAIL_SERVER=email-smtp.us-east-1.amazonaws.com
# MAIL_PORT=587

# Security
# SECRET_KEY=your-very-secret-key-here-use-secrets.token_urlsafe(32)

# ===========================================
# AMAZON SES SETUP INSTRUCTIONS
# ===========================================
# 1. Go to AWS SES Console
# 2. Verify your domain or email address
# 3. Create SMTP credentials
# 4. Use the SMTP username and password above
# 5. Make sure your domain is out of sandbox mode for production

# ===========================================
# ENVIRONMENT SWITCHING
# ===========================================
# Development: No .env file needed (uses MailHog automatically)
# Production: Set FLASK_ENV=production and add SES credentials
